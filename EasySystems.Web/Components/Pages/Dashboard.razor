@page "/dashboard"
@rendermode InteractiveServer
@inject HttpClient Http
@inject IJSRuntime JS

<PageTitle>Dashboard</PageTitle>

<div class="customer-dashboard">

    <section class="dashboard-hero-pro">
        <div>
            <p class="eyebrow">Customer Dashboard</p>
            <h1>Welcome back</h1>
            <p>Track your online store projects, current progress, and next steps.</p>
        </div>

        <a href="/create-store" class="dashboard-cta">Create new request</a>
    </section>

    @if (!string.IsNullOrWhiteSpace(message))
    {
        <div class="admin-message">@message</div>
    }

    <section class="dashboard-stats">
        <div><span>@requests.Count</span><p>Total requests</p></div>
        <div><span>@requests.Count(x => x.Status == "Pending")</span><p>Pending</p></div>
        <div><span>@requests.Count(x => x.Status == "In Progress")</span><p>In Progress</p></div>
        <div><span>@requests.Count(x => x.Status == "Completed")</span><p>Completed</p></div>
    </section>

    <section class="dashboard-content-grid">

        <div class="dashboard-main-panel">
            <h2>Your projects</h2>

            @if (!requests.Any())
            {
                <div class="empty-dashboard">
                    <h2>No requests yet</h2>
                    <p>Create your first online store request and we’ll review it.</p>
                    <a href="/create-store" class="dashboard-cta">Start now</a>
                </div>
            }
            else
            {
                @foreach (var request in requests)
                {
                    <article class="project-card-pro">
                        <div class="project-card-top">
                            <div>
                                <span class="status-pill @GetStatusClass(request.Status)">
                                    @request.Status
                                </span>

                                <h3>@request.StoreName</h3>
                                <p>@request.BusinessType</p>
                            </div>

                            <div class="package-badge">
                                @request.PackageName
                            </div>
                        </div>

                        <div class="progress-timeline">
                            @foreach (var step in statusSteps)
                            {
                                <div class="timeline-step @(IsStepActive(request.Status, step) ? "active" : "")">
                                    <div class="timeline-dot"></div>
                                    <span>@step</span>
                                </div>
                            }
                        </div>

                        <div class="project-footer-pro">
                            <span>Submitted: @request.CreatedAtUtc.ToLocalTime().ToString("yyyy-MM-dd HH:mm")</span>
                            <span>Request ID: #@request.Id</span>
                        </div>
                    </article>
                }
            }
        </div>

        <aside class="dashboard-side-panel">
            <h2>Next steps</h2>

            <div class="next-step-box">
                <strong>1. We review your request</strong>
                <p>Our team checks your store details and package.</p>
            </div>

            <div class="next-step-box">
                <strong>2. We contact you</strong>
                <p>You receive updates by email when your project status changes.</p>
            </div>

            <div class="next-step-box">
                <strong>3. Your store gets built</strong>
                <p>Once approved, we start preparing your online store.</p>
            </div>
        </aside>

    </section>
</div>

@code {
    private string message = "";
    private List<MyStoreRequestViewModel> requests = new();

    private readonly string[] statusSteps =
    {
        "Pending",
        "In Review",
        "Approved",
        "In Progress",
        "Completed"
    };

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await LoadMyRequests();
            StateHasChanged();
        }
    }

    private async Task LoadMyRequests()
    {
        var token = await JS.InvokeAsync<string>("localStorage.getItem", "authToken");

        if (string.IsNullOrWhiteSpace(token))
        {
            message = "Please login first.";
            return;
        }

        using var httpRequest = new HttpRequestMessage(
            HttpMethod.Get,
            "https://localhost:7066/api/StoreRequests/my");

        httpRequest.Headers.Authorization =
            new System.Net.Http.Headers.AuthenticationHeaderValue("Bearer", token);

        var response = await Http.SendAsync(httpRequest);

        if (!response.IsSuccessStatusCode)
        {
            message = $"Could not load your requests. Status: {(int)response.StatusCode}";
            return;
        }

        var result = await response.Content.ReadFromJsonAsync<List<MyStoreRequestViewModel>>();

        requests = result ?? new();
        message = "";
    }

    private bool IsStepActive(string currentStatus, string step)
    {
        var currentIndex = Array.IndexOf(statusSteps, currentStatus);
        var stepIndex = Array.IndexOf(statusSteps, step);

        if (currentStatus == "Rejected")
            return step == "Pending";

        return currentIndex >= stepIndex && currentIndex >= 0;
    }

    private string GetStatusClass(string status)
    {
        return status switch
        {
            "Pending" => "status-pending",
            "In Review" => "status-review",
            "Approved" => "status-approved",
            "In Progress" => "status-progress",
            "Completed" => "status-completed",
            "Rejected" => "status-rejected",
            _ => "status-pending"
        };
    }

    public class MyStoreRequestViewModel
    {
        public int Id { get; set; }
        public string StoreName { get; set; } = "";
        public string BusinessType { get; set; } = "";
        public string PackageName { get; set; } = "";
        public string Status { get; set; } = "";
        public DateTime CreatedAtUtc { get; set; }
    }
}